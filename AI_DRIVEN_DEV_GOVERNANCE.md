# AI駆動開発 組織標準プロセス（ガバナンス方法論）

> **文書の目的**
> 非エンジニアが Claude 等の AI を使って開発を行う際に、
> 会社組織として安全・品質・統制を担保するためのプロセス標準を定義する。
> 本文書は `HOW_TO_DEVELOP.md`（実践手順）と対になる「統制層」の文書である。

---

## 【意見具申】この方法論の前提修正について

> 本方法論の初版（§1〜§10）は「文書と人間の自律的遵守」を前提とした統制設計になっている。
> しかしこれには構造的な弱点がある。以下の意見具申を踏まえ、§11 で方法論を拡張する。

### 前提修正1：「統制下の自由」は文書では実現できない

**初版の前提（誤り）**: 文書でルールを定義し、認定プログラムで人を訓練すれば統制が機能する  
**修正後の認識**: 文書統制は「人が覚えていなければ機能しない」という根本的脆弱性を持つ

「統制下の自由」の本質は逆説的である。

```
文書統制モデル:
  自由 ─────────────→ 違反のリスク
  　　　　（人間の自律性に依存）

技術強制モデル:
  制約を技術が引き受ける → 人間は自由に動ける → 違反が構造的に起きない
```

非エンジニアが「Claudeに自由に頼める」状態でありながら統制が機能するには、
**人間が守るべきルールをシステムが代替執行する層**が必要である。
Skills・Harness・MCP がその層を担う。

### 前提修正2：このリポジトリのライブラリは「品質認定コンポーネント」として機能させる

本リポジトリの `HttpClient.gs`, `LazyTemplate.gs`, `SlackClient.gs` 等は、
REVIEW.md が示す通り全モジュールが A〜A- 評価を受けた品質保証済みライブラリである。

これを「非エンジニアが Claude 経由で利用できる認定コンポーネント群」として位置づけることで、
**非エンジニアが一から作るよりも安全・高品質なものを使える環境**を提供できる。
MCPとSkillsがこの「認定コンポーネントへの誘導」を自動化する。

### 前提修正3：認定プログラムの焦点を変える

**初版の焦点（誤り）**: 「HOW_TO_DEVELOP.md を理解しているか」（プロセス知識）  
**修正後の焦点**: 「何をClaudeに頼むべきか・何を頼んではいけないか」（目標設定能力と情報判断）

Skillsがプロセスを自動実行するため、非エンジニアはプロセスの手順を知る必要がない。
必要なのは「良いゴール設定」と「情報の取り扱い判断」である。

---

## 1. 前提認識：AI駆動開発が組織にもたらすリスク構造の変化

### 1.1 従来開発との根本的な違い

| 観点 | 従来のエンジニア開発 | AI駆動開発（非エンジニア） |
|---|---|---|
| コード生成者 | 人間（説明責任が明確） | AI（生成物の責任帰属が曖昧） |
| 品質の担保者 | エンジニア自身 + レビュアー | AIのレビュー + 人間の承認 |
| 問題発見のタイミング | コーディング中〜レビュー中 | **AIが見逃した問題を誰も気づかない** |
| 知識の境界 | 書いた人が理解している | **書いた人が理解していない可能性がある** |
| 変更の追跡 | コミットメッセージ＋設計意図 | AIが書いたコミットメッセージの正確性依存 |

### 1.2 AI駆動開発固有の組織リスク

```
┌─────────────────────────────────────────────────────────┐
│ リスク1：過信リスク                                         │
│   AIが「問題なし」と言ったものを無条件に信頼する               │
│   → バグ・セキュリティ脆弱性の見落とし                        │
├─────────────────────────────────────────────────────────┤
│ リスク2：情報漏洩リスク                                      │
│   機密情報・個人情報をAIに渡してしまう                         │
│   → プロンプトに含まれた情報の外部流出                        │
├─────────────────────────────────────────────────────────┤
│ リスク3：属人化リスク                                        │
│   「Claudeに聞けば分かる」で知識が組織に蓄積されない            │
│   → AIサービス停止時に誰も保守できない                        │
├─────────────────────────────────────────────────────────┤
│ リスク4：統制喪失リスク                                      │
│   誰でも・何でも・好き勝手に開発できてしまう                    │
│   → 標準・セキュリティ要件からの逸脱が発覚しない                │
└─────────────────────────────────────────────────────────┘
```

### 1.3 このリポジトリが示す「解けている部分」と「解けていない部分」

**解けている部分（このリポジトリの実証）**
- CODING_RULES.md による一貫性の担保
- REVIEW.md による指摘管理と追跡可能性
- PR単位の承認フローによる変更管理
- 「指摘0件」という客観的な品質ゲート

**解けていない部分（文書統制層のみでは解けない）**
- 誰がAI開発をしてよいか（認可・認定） → **Harness の権限スコープで技術的に解決**
- 何をAIに渡してよいか（データ分類） → **Harness の Hook で事前検査して技術的に解決**
- どのシステムにAI開発を適用してよいか（リスク階層） → **Tier 別 Harness 設定で技術的に解決**
- 問題が起きたときの責任体制 → **文書統制が引き続き必要**
- 監査証跡の標準化 → **Hook による自動付記と MCP 経由の操作ログで解決**
- 品質保証済みコンポーネントへの誘導 → **Skills と MCP が自動推薦することで解決**

---

## 2. 役割定義（RACI モデル）

### 2.1 登場人物

| 役割 | 略称 | 定義 |
|---|---|---|
| **AI開発者** | Dev | AIを使って開発を行う担当者（非エンジニアを含む） |
| **技術承認者** | Tech | PRをレビュー・マージ承認できるエンジニア |
| **プロジェクトオーナー** | PO | 開発の目的・要件・優先度に責任を持つ人 |
| **情報セキュリティ担当** | Sec | データ分類・セキュリティ要件の管理者 |
| **品質管理担当** | QA | 品質基準の定義・確認をする人 |
| **組織管理者** | Mgr | AI開発プロセス全体を統括・改善する人 |

### 2.2 主要アクティビティのRACI

| アクティビティ | Dev | Tech | PO | Sec | QA | Mgr |
|---|---|---|---|---|---|---|
| CODING_RULES.md の策定 | C | **R** | C | C | C | A |
| AI開発者の認定 | I | C | I | C | C | **A/R** |
| 開発対象のリスク判定 | I | C | **R** | C | I | A |
| AIへの情報提供可否判断 | C | C | C | **R** | I | A |
| コード生成・修正 | **R** | I | I | I | I | I |
| PRの技術レビュー | C | **R** | I | C | C | I |
| PRの承認・マージ | I | **A/R** | C | I | I | I |
| セキュリティレビュー | I | C | I | **R** | I | A |
| 本番リリース承認 | I | C | **R** | C | C | A |
| インシデント対応 | C | **R** | A | R | C | A |
| プロセス改善 | C | C | C | C | C | **A/R** |

*R=実行責任, A=説明責任, C=相談, I=情報受領*

---

## 3. システムリスク階層（Tier制）

すべての開発対象に同じ統制を適用するのは非効率。
対象システムの重要度に応じて統制レベルを変える。

### 3.1 Tier定義

| Tier | 対象の特性 | 例 |
|---|---|---|
| **Tier 1**（高リスク） | 個人情報・機密情報を扱う / 外部公開 / 金銭的影響 | 顧客DB連携、決済処理、認証系 |
| **Tier 2**（中リスク） | 社内業務に影響 / 内部ツール | 社内申請ツール、レポート自動化 |
| **Tier 3**（低リスク） | 個人業務効率化 / 試作品 | スプレッドシートの集計スクリプト |

このリポジトリ（GASライブラリ）は典型的な **Tier 2〜3** に相当する。

### 3.2 Tierごとの必須統制

| 統制項目 | Tier 1 | Tier 2 | Tier 3 |
|---|---|---|---|
| CODING_RULES.md の適用 | 必須 | 必須 | 推奨 |
| REVIEW.md による指摘管理 | 必須（全版保存） | 必須 | 推奨 |
| 技術承認者のPRレビュー | 必須（2名以上） | 必須（1名以上） | 推奨 |
| セキュリティレビュー | 必須 | リリース前1回 | 不要 |
| テストコードの作成 | 必須（カバレッジ基準あり） | 必須 | 推奨 |
| 本番リリース前の品質ゲート | 指摘H=0, M=0 | 指摘H=0 | 任意 |
| 変更管理票の起票 | 必須 | 必須 | 不要 |
| AIへの入力データの事前審査 | 必須 | 推奨 | 不要 |

### 3.3 Tier 別 Harness 設定（§11 参照）

Tier ごとに Claude Code の `settings.json` を分け、**文書ではなく設定ファイルで権限を強制する**。

```
Tier 1 プロジェクト:
  .claude/settings.json
    allowedTools: [MCP経由のみ、直接Bash禁止]
    permissions: denyList に本番系パス・DB接続文字列を含む
    hooks: PreToolUse → データ分類チェック必須

Tier 2 プロジェクト:
  .claude/settings.json
    allowedTools: [Bash, Edit, Write, 組織内MCP]
    hooks: PostToolUse → REVIEW.md 更新を促す通知

Tier 3 プロジェクト:
  .claude/settings.json（最小限）
    allowedTools: 標準ツール一式
    特別な制限なし
```

Harness 設定はプロジェクトリポジトリ（`.claude/`）に含めてバージョン管理する。
「このプロジェクトは Tier 2 である」という事実が、設定ファイルとして技術的に具現化される。

---

## 4. データ・情報の取り扱い基準

### 4.1 AIに渡してよい情報・渡してはいけない情報

| 分類 | 内容 | AIへの提供 |
|---|---|---|
| **公開情報** | OSS、公開ドキュメント、汎用コード | ✅ 自由に提供可 |
| **社内一般情報** | 業務フロー説明、画面仕様書（個人情報なし） | ✅ 原則提供可 |
| **社内機密情報** | 戦略文書、契約情報、財務情報 | ⚠️ Sec 承認後のみ |
| **個人情報（社員）** | 氏名・メアド・人事情報 | ❌ 原則禁止 |
| **個人情報（顧客）** | 顧客ID・購買履歴・連絡先 | ❌ 禁止 |
| **認証情報** | パスワード・APIキー・トークン | ❌ 絶対禁止 |

### 4.2 コードレビュー依頼時の注意

```
✅ OK な依頼の例:
  「このコードをレビューしてください」（コードのみを貼る）

⚠️ 要注意な依頼の例:
  「顧客テーブルの構造はこうなっています」
  → テーブル定義にカラム名が含まれ、設計情報が漏洩しうる

❌ NG な依頼の例:
  「本番DBのこのデータで動作確認してください」
  → 実データを渡してはいけない
```

### 4.3 プロンプトのログ管理

- AI開発で使用した主要プロンプト（依頼文）は Git コミットメッセージか Pull Request の description に残す
- 「何をAIに頼んだか」が追跡可能であること

---

## 5. 品質ゲート設計

### 5.1 レビューの客観的な完了基準

このリポジトリが実証した「指摘0件をゴールにする」を組織標準に昇格させる。

```
品質ゲート（Tier 2 の場合）

  ┌─────────────────────────────┐
  │ REVIEW.md 最新版が存在する    │ ← 存在しなければレビュー未実施
  ├─────────────────────────────┤
  │ H（High）指摘が 0件である     │ ← バグ・データ損失リスクがない
  ├─────────────────────────────┤
  │ 未解決の M（Medium）指摘が    │ ← 「設計意図によりクローズ」は可
  │ すべて方針決定済みである      │
  ├─────────────────────────────┤
  │ テストコードが存在する        │ ← 動作確認の再現性
  └─────────────────────────────┘
         ↓ 全て満たす
     本番リリース承認へ進める
```

### 5.2 指摘の重要度定義（組織標準版）

このリポジトリの REVIEW.md から組織標準として抽象化する。

| ランク | 定義 | リリース可否 |
|---|---|---|
| **H（High）** | バグ、セキュリティ脆弱性、データ損失・漏洩リスク | **リリース不可** |
| **M（Medium）** | 堅牢性・保守性の問題。方針決定が必要 | 方針決定後に可 |
| **L（Low）** | スタイル・改善提案。影響軽微 | リリース可（次版で対応） |
| **設計意図クローズ** | 技術承認者が「直さないことを承認した」もの | リリース可（理由の記録必須） |

### 5.3 セキュリティ観点の追加チェック（Tier 1/2 必須）

AIはコードの論理的正しさは高精度でレビューできるが、
組織固有のセキュリティ要件は別途確認が必要。

```
□ 外部から入力を受け取る箇所にバリデーションがあるか
□ 認証・認可の抜け漏れがないか
□ APIキー・パスワードがコードにハードコードされていないか
□ ログに個人情報・機密情報が出力されないか
□ 依存ライブラリに既知脆弱性がないか（OSS使用時）
```

---

## 6. 監査証跡の標準化

### 6.1 Git リポジトリを「証跡の器」として使う

| 証跡 | 保存場所 | 内容 |
|---|---|---|
| 変更履歴 | Git コミット | 何をいつ変更したか |
| レビュー記録 | REVIEW.md（累積版） | 何を指摘し、どう対応したか |
| 承認記録 | PR の Merge 履歴 | 誰がいつ承認したか |
| 設計判断 | PR description / REVIEW.md §「設計意図によりクローズ」 | なぜ直さないのかの根拠 |
| AIへの依頼内容 | PR description / コミットメッセージ | Claudeに何を頼んだか |

### 6.2 REVIEW.md の必須記載事項（組織標準版）

```markdown
# レビュー記録（累積版）

## レビュー版一覧
| 版 | 対象 | レビュー日 | 実施者 | 承認者 | 要対応残 |
                                ↑技術承認者名  ↑PO名

## 指摘事項
各指摘に以下を必ず記載:
- 指摘ID
- 重要度（H/M/L）
- 対象ファイル・行番号
- 内容
- 対応ステータス（未対応/修正済み/設計意図クローズ）
- クローズ理由（設計意図クローズの場合）
```

### 6.3 コミットメッセージの標準形式

```
<種別>(<対象>): <概要>

[変更の背景・理由]（AIへの依頼内容を含む場合はここに記載）

REVIEW.md 第N版: <指摘ID>対応
```

---

## 7. AI開発者の認定プログラム

### 7.1 認定の必要性

誰でも無制限にAI開発ができる状態は統制喪失リスクに直結する。
「AI開発認定」という資格制度で入口を管理する。

### 7.2 認定要件（改訂版：プロセス知識から目標設定能力へ）

> **初版からの修正**: Skills がプロセスを自動実行するため、非エンジニアは手順の暗記は不要。
> 必要なのは「何をClaudeに頼むか」という目標設定能力と「何を渡してはいけないか」という情報判断力。

```
レベル1（Tier 3 のみ適用可）
  ─ 目標設定能力 ─
  □ 「要件を具体的に言語化する」実習を修了
  □ 「曖昧な指示がなぜ問題か」を説明できる

  ─ 情報判断力 ─
  □ 情報分類基準（§4.1）の研修を修了
  □ AIに渡してよい情報・渡してはいけない情報を3例ずつ挙げられる

  ─ 実習 ─
  □ Tier 3 Harness 設定済みのサンドボックスで1サイクル完走

レベル2（Tier 2 まで適用可）
  □ レベル1の条件を満たす
  □ PRを1本マージした実績がある（内容を技術承認者が確認済み）
  □ 「設計意図によりクローズ」の判断を技術承認者と1回以上行った実績
  □ Skill の呼び出し方と結果の読み方を説明できる

レベル3（Tier 1 も適用可）
  □ レベル2の条件を満たす
  □ セキュリティ基礎研修を修了
  □ 過去6ヶ月以内にセキュリティインシデントを起こしていない
  □ 組織管理者の個別承認
```

### 7.3 認定の維持

- 年1回の更新（プロセス改訂への追従）
- インシデントを起こした場合は再研修が必要
- AIツールの仕様変更時は臨時更新

---

## 8. インシデント対応フロー

### 8.1 AI生成コードに問題が発生したときの初動

```
問題発見
   ↓
1. 影響範囲の確認（どのシステム・データに影響するか）
   ↓
2. 暫定対応（サービス停止 or ロールバック）
   ↓
3. 技術承認者・Secへの報告（1時間以内）
   ↓
4. REVIEW.md に「事後レビュー」として原因分析を追記
   ↓
5. 恒久対応の計画と実施
   ↓
6. プロセス改善提案（再発防止策をMgrへ提出）
```

### 8.2 「AIが見逃した」は免責にならない

AI開発において「AIがそう言ったから」は免責にならない。
PRを承認した**技術承認者**と、開発を指示した**POが説明責任を負う**。
この認識を組織全体で共有することがインシデント予防の基本となる。

---

## 9. 導入ロードマップ

### フェーズ1（準備期：1〜2ヶ月）

```
□ 組織管理者（Mgr）の任命
□ このリポジトリをモデルケースとして内部展開
□ CODING_RULES.md テンプレートの整備（業務領域別）
□ 情報分類基準の確定（§4.1）
□ Tier定義を自社のシステム台帳に適用
```

### フェーズ2（試行期：2〜4ヶ月）

```
□ 希望者でレベル1認定の試行
□ Tier 3 のみで AI開発を試験運用
□ REVIEW.md の記載品質を技術承認者がフィードバック
□ 問題点を収集してプロセスを修正
```

### フェーズ3（展開期：4〜8ヶ月）

```
□ レベル2認定の開始
□ Tier 2 への適用拡大
□ 月次でプロセス適合状況をレビュー
□ ナレッジベース（FAQ・典型的なCODING_RULES.md集）の整備
```

### フェーズ4（定着期：8ヶ月〜）

```
□ Tier 1 への適用検討（Sec・法務を含む検討会で判断）
□ 定期的なプロセス監査（年1回）
□ AI技術の進化に合わせた規程改訂サイクルの確立
```

---

## 10. よくある問題と対処パターン

### Q1. 「Claudeに頼めばすぐできる」という過信が広まっている

**対処**:
- 品質ゲート（§5.1）を厳格に運用し「レビューなしのリリース」を組織として禁止
- インシデント事例を（匿名化して）共有し、AIが見落とす種類の問題を実感させる

### Q2. 技術承認者のリソースがボトルネックになる

**対処**:
- Tier制を運用し、Tier 3は技術承認者なしで動ける範囲を明確化
- 繰り返し出る問題パターンをCODING_RULES.mdに追加してAI側で防がせる
- レビュー負荷をメトリクス化してMgrが調整できるようにする

### Q3. REVIEW.mdが形骸化して「コピペ確認」になる

**対処**:
- 「設計意図によりクローズ」には必ず理由の記述を義務化（理由なしは不可）
- 技術承認者のレビューコメントをPR上に残すことを必須化
- 季次監査でREVIEW.mdの品質をサンプル確認する

### Q4. AIが間違ったことを自信を持って答えることがある

**対処**:
- 「AIの出力は必ず検証する」という原則をプロセスに明記
- 複数のAIに同じ問題を聞いて答えを比較する手法を研修に組み込む
- 重要な設計判断は技術承認者が独立して判断する（AIの意見はあくまで参考）

---

## 付録A：このリポジトリが示す「理想的な実装例」との対応表

| このガバナンス文書の要素 | リポジトリでの実装 |
|---|---|
| CODING_RULES.md（§1.3） | `CODING_RULES.md` |
| REVIEW.md 累積版（§6.1） | `REVIEW.md`（第1〜9版） |
| 品質ゲート「H=0件」（§5.1） | 各PR説明「全指摘0件達成」 |
| 設計意図クローズの記録（§5.2） | REVIEW.md §「設計意図によりクローズした指摘」 |
| PR承認フロー（§2.2） | 15本のPRのMerge履歴 |
| 利用者向けドキュメント（§5.1） | `loadAsObjects.md` |
| ブランチ命名による追跡（§6.1） | `claude/review-*` `claude/add-*` |

---

---

## 11. 技術強制層：Skills・Harness・MCP

> §1〜§10 は「文書統制層」である。本節は「技術強制層」を定義する。
> 両者が重なることで「統制下の自由」が実現する。

```
┌──────────────────────────────────────────────────────┐
│                    非エンジニア                          │
│              「Claudeに自由に話しかける」                  │
└─────────────────────┬────────────────────────────────┘
                      │ 自然言語の指示
┌─────────────────────▼────────────────────────────────┐
│               技術強制層（本節）                          │
│                                                        │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐   │
│  │  Skills   │  │ Harness  │  │      MCP         │   │
│  │（工程標準化）│  │（権限強制）│  │（組織接続の制御）  │   │
│  └──────────┘  └──────────┘  └──────────────────┘   │
└─────────────────────┬────────────────────────────────┘
                      │ 統制された操作のみ通過
┌─────────────────────▼────────────────────────────────┐
│               文書統制層（§1〜§10）                      │
│  CODING_RULES.md / REVIEW.md / Tier制 / RACI         │
└──────────────────────────────────────────────────────┘
```

---

### 11.1 Skills（スキル）— 工程を自動標準化する

#### Skills とは

Claude Code において `/review` や `/security-review` のように、
スラッシュコマンドとして呼び出せる**定型ワークフローのカプセル化**である。

非エンジニアは「何をすべきか」を知らなくても、
スキルを呼ぶだけで**正しいプロセスが自動実行される**。

#### このリポジトリで有効なスキルの設計例

| スキル名 | 呼び出し | 実行されること |
|---|---|---|
| `/review` | レビューしてほしいとき | CODING_RULES.md を読み込み → REVIEW.md に H/M/L 形式で指摘を追記 → 修正 PR を作成 |
| `/security-review` | リリース前の最終確認 | §5.3 のセキュリティチェックリストを自動実行 → 結果を REVIEW.md に記録 |
| `/init-project` | 新プロジェクト開始時 | CODING_RULES.md / REVIEW.md の雛形を生成 → Tier に応じた `.claude/settings.json` を生成 |
| `/use-library` | 既存ライブラリを使うとき | このリポジトリの認定済みライブラリ一覧を提示 → 選択肢の品質評価（REVIEW.md）を要約 |

#### Skills が解決する問題

```
問題: REVIEW.md の書き方が人によって違い、形骸化する
解決: /review スキルが常に同じフォーマットで出力する

問題: 技術承認者が忙しく、レビュー依頼のタイミングが曖昧
解決: /review が自動で PR を作るので、承認待ちの状態が可視化される

問題: 新人がどのスキルを使えばいいか分からない
解決: /help スキルが現在の作業状況に応じて次のスキルを案内する
```

#### Skills の実装場所

```
.claude/
  commands/           ← プロジェクト固有のスキル定義
    review.md         ← /review スキルの指示文
    security-review.md
    init-project.md
    use-library.md
```

スキルファイルはリポジトリに含めてバージョン管理する。
「このプロジェクトでは /review を使う」というルールが、設定として強制される。

---

### 11.2 Harness（ハーネス）— 権限を技術的に強制する

#### Harness とは

Claude Code の実行環境を管理する設定・フックの集合。
`.claude/settings.json` で**Claude が実行できる操作の範囲を事前に宣言**し、
フック（hooks）で**操作の前後に自動チェックを挟む**。

#### 非エンジニアが「やってはいけないこと」を技術的に不可能にする

```javascript
// .claude/settings.json（Tier 2 プロジェクトの例）
{
  "permissions": {
    "allow": [
      "Bash(git:*)",           // git 操作は許可
      "Bash(node:*)",          // ローカル実行は許可
      "Edit(**/*.gs)",         // GAS ファイルの編集は許可
      "Edit(**/*.md)"          // ドキュメントの編集は許可
    ],
    "deny": [
      "Bash(curl:*)",          // 外部への直接通信は禁止
      "Bash(rm -rf:*)",        // 一括削除は禁止
      "Edit(**/.env*)"         // 環境変数ファイルの編集は禁止
    ]
  }
}
```

このファイルがリポジトリに存在するだけで、
**Claude がルール違反の操作を要求されても実行できない状態**になる。

#### フック（Hooks）による自動チェック

```
PreToolUse フック（ツール実行前）:
  → ファイルへの Write 操作前に「対象パスが機密ファイルでないか」を確認
  → git push 前に「REVIEW.md が存在し、H指摘が0件か」を確認
  → 外部API呼び出し前に「MCPを通じた操作か否か」を確認

PostToolUse フック（ツール実行後）:
  → コミット後に「コミットメッセージが標準形式か」を確認
  → PR作成後に「REVIEW.md の最新版がリンクされているか」を確認

Stop フック（セッション終了時）:
  → 未コミットの変更がないか確認し、あれば警告を表示
```

#### 監査証跡の自動付記

```
PostToolUse フック（コミット操作後）:
  コミットメッセージに以下を自動追記:
    - 実行日時
    - セッションID（Claudeとの会話を特定できる情報）
    - 使用した主要スキル名
```

これにより「誰がいつどのようにClaudeを使ったか」が、
人間の入力に依存せず自動的に記録される。

---

### 11.3 MCP（Model Context Protocol）— 組織システムへの制御された接続

#### MCP とは

Claude が外部システムと連携するための**標準化されたプラグイン機構**。
生の API キーや直接接続ではなく、**MCP サーバーが仲介役となる**ことで
アクセスの範囲・ログ・認証を一元管理できる。

このセッション自体も GitHub MCP を通じて `aokazaki-olp/libraries` リポジトリのみに
アクセスが制限されている。これが MCP の統制機能の実例である。

#### 組織内 MCP の設計例

| MCP サーバー | 提供する機能 | 統制の内容 |
|---|---|---|
| **GitHub MCP** | PR作成・コード管理 | 操作対象リポジトリを許可リストで制限 |
| **社内DB MCP** | データ参照 | 読み取り専用・個人情報カラムを自動マスク |
| **認定ライブラリ MCP** | このリポジトリの利用 | 品質評価済みコンポーネントのみ提案 |
| **変更管理 MCP** | 変更管理票の起票 | Tier 1/2 のリリース前に自動起票 |
| **Slack MCP** | 承認依頼の通知 | 技術承認者への通知を自動送信 |

#### データ分類の技術的実装

§4.1 の情報分類ルールを「文書」から「技術制御」に昇格させる。

```
社内DB MCP の設計:
  - customers テーブルへのアクセス → email, phone カラムをマスク返却
  - contracts テーブルへのアクセス → Sec 承認トークンがなければ拒否
  - logs テーブルへのアクセス → 読み取り専用、30日以内のデータのみ

→ 「個人情報をAIに渡してはいけない」というルールが
   MCPサーバーによって技術的に執行されるため、
   非エンジニアが「うっかり渡す」ことが構造的に起きない。
```

---

### 11.4 認定ライブラリの活用：品質保証済みコンポーネント戦略

#### このリポジトリをコンポーネントカタログとして使う

本リポジトリのライブラリは REVIEW.md で品質評価済み（全モジュール A〜A-）。
これを非エンジニアが Claude 経由で活用できる仕組みを作ることで、
「ゼロから作る」よりも安全・高品質な開発基盤を提供できる。

```
非エンジニアの指示:
  「Slack にメッセージを送るスクリプトを作りたい」

Skillsによる自動誘導（/use-library の動作）:
  → 認定ライブラリカタログを参照
  → SlackWebhookClient（A評価）を発見
  → 「このライブラリを使うことを推薦します。品質評価: A」と提示
  → 使い方のサンプルコードを生成

→ 非エンジニアは「品質保証済みの部品」を使ってコードを書ける
```

#### 認定コンポーネントの登録・更新フロー

```
1. エンジニアが新しいライブラリを REVIEW.md でレビュー
2. H=0件、品質 B+ 以上 → 「認定コンポーネント」としてカタログに登録
3. MCP / Skill がカタログを参照し、非エンジニアに自動推薦
4. ライブラリに更新があれば再レビューを実施、評価を更新
```

---

### 11.5 「統制下の自由」の実現原理

#### 文書統制モデル vs 技術強制モデルの比較

| 観点 | 文書統制（§1〜§10のみ） | 技術強制（§11 追加後） |
|---|---|---|
| 違反の起きやすさ | 人が忘れれば起きる | 構造的に起きにくい |
| 非エンジニアの認知負荷 | ルールを覚える必要がある | 覚えなくてよい（Skillが代行） |
| 品質の一貫性 | 人によってバラつく | Skillが常に同じ手順を実行 |
| 監査証跡の正確性 | 人の入力に依存 | Hookが自動付記 |
| 非エンジニアの自由度 | ルールの範囲内で制限される | **ルールを意識せず動ける＝自由** |

#### 非エンジニアが感じる「自由」の正体

技術強制モデルでは、非エンジニアは：
- 「どうやってREVIEW.mdを書くか」を知らなくてよい → `/review` が書く
- 「何がTier 1の制限か」を覚えなくてよい → Harnessが拒否する
- 「このAPIキーを渡していいか」を判断しなくてよい → MCPが防ぐ
- 「品質の良いライブラリはどれか」を調べなくてよい → `/use-library` が提案する

**その分、「何を作りたいか」「どんな価値を生み出すか」に集中できる。**
これが「統制下の自由」の本質である。

---

### 11.6 Skills・Harness・MCP の研究・実装ロードマップ

> 本節は研究フェーズにある。以下は方向性の整理であり、実装詳細は基礎研究の進捗に応じて更新する。

#### フェーズ0（基礎研究）— 現在

```
□ Skills の仕様理解（スキルファイルの形式・呼び出し機構）
□ Harness の仕様理解（settings.json・hooks の設計）
□ MCP の仕様理解（サーバー実装・接続設定）
□ Claude Code の権限モデルの全体把握
□ このリポジトリを使ったプロトタイプ検証
```

#### フェーズ1（最小構成の実装）

```
□ /review スキルの作成（REVIEW.md の自動フォーマット生成）
□ Tier 3 の Harness 設定（最小限の制限）
□ GitHub MCP の組織内展開設定
□ このリポジトリを認定ライブラリとして /use-library スキルに登録
```

#### フェーズ2（本格展開）

```
□ /security-review スキルの作成
□ Tier 1/2 の Harness 設定（段階的な制限強化）
□ 社内DB MCP の設計・実装（データマスク機能含む）
□ 認定ライブラリカタログの整備
□ Hook による監査証跡自動付記
```

#### フェーズ3（定着・進化）

```
□ Skill 利用状況のメトリクス収集
□ 繰り返し起きる問題をSkillに吸収（自己改善ループ）
□ 新しいAIモデル・APIへの追従
□ 組織固有のドメイン知識をMCPに蓄積
```

---

## 付録B：統制の実装マッピング（改訂版）

| ガバナンス要件 | 文書統制 | 技術強制（Skills/Harness/MCP） |
|---|---|---|
| プロセスの一貫性 | CODING_RULES.md | `/review` スキルが毎回同じ手順を実行 |
| 品質ゲートの客観性 | REVIEW.md 指摘H=0件 | Hook が push 前に自動確認 |
| データ分類の遵守 | §4.1 の一覧表 | MCP サーバーがアクセスを技術的に制限 |
| Tier 別権限管理 | §3 のルール表 | `.claude/settings.json` でファイルレベル強制 |
| 監査証跡の正確性 | §6.3 コミット形式 | PostToolUse Hook が自動付記 |
| 品質保証済み部品の活用 | ライブラリ一覧の周知 | `/use-library` スキルが自動推薦 |
| 変更管理の可視化 | PR の Merge 履歴 | 変更管理 MCP が自動起票 |
| 承認フローの強制 | §2.2 RACI | GitHub MCP の PR 必須化設定 |

---

*本文書は `HOW_TO_DEVELOP.md`（実践手順）と組み合わせて使用する。*
*組織の状況に合わせて各Tierの定義・認定要件を適宜調整すること。*
*§11 の技術強制層は基礎研究の進捗に応じて継続的に更新する。*
