# 設計思想 / Philosophy

このライブラリが何を目指し、何を目指さないかを記録する文書。
`CODING_RULES.md` が「どう書くか」を定めるのに対し、
このファイルは「なぜそう作るか/作らないか」を定める。

---

## 0. 一行で言うと

> **「ちょっとだけやりたい」が "ちょっと" のまま済む確率を上げる、GASらしい道具箱。**

SDK ではない。フレームワークでもない。
包括的なカバレッジも追わない。
金曜の午後に 30 分で書こうとしたスクリプトが 30 分で終わる、その確率を上げることだけが目的。

---

## 1. North Star: GASらしさ

### 1.1 GASらしさを操作的に定義する

「GASらしい」は雰囲気で済ませると抽象化して死ぬので、次の具体的な肌感で定義する。

> **取り込んだことで増えるコードより、取り込んだことで消えるコードのほうが多い状態。**

SDK は逆の交換条件をとる。初期化ボイラープレートが先に増え、代わりに長期的な構造化が手に入る。
このライブラリはその交換条件を採らない。

### 1.2 GASらしさテスト(3 問)

新しい API を追加するとき、既存 API を変更するとき、この 3 問を通過することを要件とする。

1. **1〜2 行で最初のご利益が出るか?**
   - 良い例: `loadFromSheetAsObjects(sheet)`
   - 良い例: `WebhookClient.send(url, { text: 'hello' })`
   - 良い例: `new LazyTemplate(tpl).evaluate(data)`

2. **初期化ボイラープレートがゼロに近いか?**
   - DI コンテナ、設定ファイル、`init()` 呼び出しを要求しない。
   - `Xxx.create(...)` か `new Xxx(...)` の 1 行で使い始められること。

3. **取り込まずに書いた GAS スクリプトと "見た目が地続き" か?**
   - ライブラリの関数が、標準 GAS API(`SpreadsheetApp`, `UrlFetchApp`, ...)の隣に並んでも違和感のない語彙であること。
   - `new SheetRepositoryFactoryBuilder()` のような語彙が混ざった瞬間に GAS らしさは死ぬ。

3 問全部に合格しない API は、このライブラリの一部ではない。

### 1.3 価値の測り方

SDK の価値は **カバレッジ** で測られる(API の何%を網羅しているか)。
このライブラリの価値はそれとは違う軸で測る。

> **「ちょっとだけやりたい」が "ちょっと" のまま済む確率。**

カバレッジが低いことは欠点ではない。1 行で済む肌触りが崩れたときだけが欠点。

---

## 2. YAGNI の質

### 2.1 Feature に対する YAGNI、Seam に対する非 YAGNI

世の中の YAGNI コードには 2 種類ある。

- **良い YAGNI**: 今必要な機能だけ作る。ただし将来の剥がれ線(seam)は設計に織り込む。
- **悪い YAGNI**: 今必要な機能だけ作る。剥がれ線は考えない。

このライブラリは前者を選ぶ。
**機能は必要になってから足す。ただし継ぎ目だけは先に切っておく。**

機能を先に足すのは投機的一般化(speculative generality)。
継ぎ目を先に切らないのは、後で根本改造を強いられる負債。
両者は対称ではない。

### 2.2 既に引かれている継ぎ目

このライブラリが小さいまま大きい仕事を受けられる理由は、以下の継ぎ目が最初から引かれていること。

| 継ぎ目 | 提供元 | 役割 |
|---|---|---|
| **1 メソッド契約の Transport** | `HttpCore.createTransport` | HTTP 通信の最小抽象。`{ fetch }` の 1 メソッドのみ。 |
| **イミュータブルな `extend()`** | `ApiClient` | デコレータを層々と積んでも元のクライアントを壊さない。 |
| **`responseHandler` 注入口** | `ApiClient.createClient` | API 固有のエラー形状や後処理をクライアント本体を触らずに差し込める。 |
| **`LoggerFacade` による依存逆転** | `LoggerFacade` | `console` / GAS `Logger` / Winston / `null` のどれでも受ける。 |
| **`.use()` プラグイン注入** | `ClientHelper` | ドメインメソッドをクライアント本体の外に持てる。 |

これらはどれも **機能ではなく継ぎ目** である。
機能なら YAGNI で削れるが、継ぎ目は削れない。

---

## 3. 疎結合の実装レベル

### 3.1 依存の向きの一貫性

"知る側" と "知られる側" の向きが一貫して正しいこと。

- `LazyTemplate` は `Slack` を知らない。
  `SlackFilters` / `SlackResolvers` が **後から** `LazyTemplate` に登録する側。
- `loadAsObjects` は `resolveSheet` 以外に何も知らない。
- `HttpCore` は `Slack` を知らない。
  Slack 固有のリトライ事情(`Retry-After` 尊重)は `SlackCore` という別系統に隔離される。

**基盤は上位を知らない。上位が基盤に降りて接続する。** この向きを破ってはいけない。

### 3.2 Decorator を immutable に合成

`extend()` は常に新しいクライアントを返す。元のクライアントは絶対に変更されない。
これは「1 つのクライアントに複数のデコレータ構成を使い分けたい」というまっとうな要求に対する答え。

```javascript
const base = ApiClient.createClient({ baseUrl });
const authed = base.extend(t => ApiClient.withBearerAuth(t, token));
const logged = authed.extend(t => HttpCore.withLogger(t, console));
// base は壊れていない。authed も壊れていない。
```

### 3.3 プラグインは外側から差し込む

`SlackFilters` が `LazyTemplate.Filters.Slack` に**後から**フィルタを登録する形は、
このライブラリの拡張の公式な作法である。
将来増える拡張も、同じ向き(上位が基盤に降りて接続する)で差し込むこと。

---

## 4. SDK ではない ― 深いライブラリ(deep library)を選ぶ

### 4.1 Deep vs Wide

ソフトウェア基盤には 2 系統の拡張哲学がある。

| | 広いライブラリ (wide) | 深いライブラリ (deep) ← **これ** |
|---|---|---|
| 例 | 各種公式 SDK | UNIX パイプ, このライブラリ |
| 拡張戦略 | 事前にメニューを全部提供 | 小さな核と組み立て規約を提供 |
| 表面積 | 巨大(N エンドポイント) | 小さい(1 桁のプリミティブ) |
| バージョン追従 | 常時必要 | ほぼ不要 |
| 呼び出し側の責務 | 軽い | プロトコルを理解する必要がある |
| 成長 | 線形で肥大 | ほぼ一定のまま永続 |

公式 SDK が存在する世界で、それと同じ土俵に上がろうとしない。
**このライブラリがやるのは "公式 SDK の薄い代替" ではなく、"公式 SDK が大げさすぎる現場のための、核だけある道具"** である。

### 4.2 エンドポイントは呼び出し側に属する

API のエンドポイントを網羅的に提供することはしない。
呼び出し側が `.use()` で必要なものだけ注入する。
既存の `SlackApiClient` が既にこの形であり、新しく追加されるクライアントもこの形を踏襲する。

```javascript
// 良い: 呼び出し側が必要なメソッドだけ注入
const slack = SlackApiClient.create(token)
  .use('postMessage', c => (channel, text) =>
    c.call({ endpoint: 'chat.postMessage', body: { channel, text } }));
```

### 4.3 プロトコル層 vs ドメイン層の線引き

"エンドポイントを提供しない" は "何も入れない" ではない。
**エンドポイントではないが、全呼び出し側が必ず必要とするもの** は本体に入れる。これを怠ると `.use()` の前提が崩れる。

線引きの判定基準は一文で:

> **「組織の独自項目 / ビジネス概念」に触れる瞬間、ライブラリの外側。**

| 層 | 置き場所 | 例 |
|---|---|---|
| **プロトコル層** (内側) | ライブラリ本体 | 認証デコレータ、リトライ、エラー正規化、URL ビルダ、エスケープ、ページング補助 |
| **ドメイン層** (外側) | 呼び出し側 or プラグイン | `getFoo()`, `updateBar()`, 特定フィールドに依存したあらゆるメソッド |

プロトコル層は **プロトコル仕様が決めるもの**。ドメイン層は **その組織の業務が決めるもの**。
前者は不変に近く、後者はプロジェクトごとに違う。この違いが線の場所を決める。

---

## 5. 成長の規律

### 5.1 Rule of Three

機能の昇格(本体化)は常に "3 つ目が来た瞬間" に行う。

| 状況 | 置き場所 |
|---|---|
| 1 プロジェクトだけで使っている | そのプロジェクト内 |
| 2 プロジェクトで似たものが生えた | まだプロジェクト内でよい |
| 3 プロジェクト目で同じパターンが出た | **任意 import の共有プラグインとして昇格** |

1 つ目や 2 つ目で昇格するのは投機的一般化。
4 つ目まで待つのは怠慢で、それは既にライブラリの問題。

### 5.2 昇格先は本体 API ではなく "任意 import のプラグイン"

共有されるものも、必ずしもクライアント本体のメソッドにしなくてよい。
`LazyTemplate.Filters.Slack` のように **呼び出し側が明示的に取り込むプラグイン** として置く方式を優先する。

- 取り込まないプロジェクトは影響を受けない
- 本体 API の表面積は増えない
- 呼び出し側は "何を取り込んだか" が明示される

### 5.3 成長の自然な停止点

GASらしさテスト(1.2 節)がそのまま成長停止の判定になる。

- SDK 志向のライブラリには停止点がない。エンドポイントは常に増える。**"足したい気持ち" に対して規律でしか抵抗できない**。
- GAS らしさ志向のライブラリは、1〜2 行で書ける形を崩した瞬間に違反が検知される。**"止めるべきか" の問いが客観化できる**。

何かを足したくなったら、次の一文で自己判定する:

> **「その変更は GASらしさテスト(3 問)を通過するか?」**

通過しないなら、それはこのライブラリの一部ではない。
プロジェクト側に住まわせるか、別のライブラリになるべきもの。

---

## 6. アンチパターン

以下はすべて、North Star に違反する典型的な失敗。
気づいたら押し戻す対象。

### 6.1 "よく使うから" で本体にショートカットを生やす

`client.get(buildPath(...))` の 2 行で済むことに対して `client.getRecord(...)` を作らない。
1 段のショートカットは作った瞬間、次のショートカットが生え、歯止めが効かなくなる。

### 6.2 組織固有の概念をライブラリ本体が知る

カスタム項目名、社内用語、特定プロジェクト由来のデータ構造をライブラリ本体に入れない。
これらは呼び出し側の `.use()` かプロジェクトのコードに住むべきもの。

### 6.3 レスポンスを独自クラスでラップする

API レスポンスを `SomeRecord` のような独自クラスにラップしない。
返ってくるのは素の JSON であるべき。

これは `loadAsObjects` が **「loader は切るだけ、型変換しない」** と明言している思想と同じ理由に基づく。
ラップした瞬間、呼び出し側のドメイン知識がライブラリに染み出してくる。

### 6.4 Feature の YAGNI を Seam の YAGNI に転化する

「まだ 1 つしか使っていないから継ぎ目は切らない」という判断はしない。
機能の YAGNI は合法だが、継ぎ目の YAGNI は将来の根本改造を予約する行為。

### 6.5 取り込み側の初期化コードが 5 行以上になる

ライブラリを使い始めるためのボイラープレートが肥大したら、それは設計の失敗。
セットアップが本質的に重い領域(例: OAuth2 JWT Bearer Flow の初回構成)に遭遇した場合は、
**初回セットアップを別動線(メニュー経由のヘルパ等)に逃がし、日常の呼び出し側コードには出さない**。

---

## 7. 応用の例 ― 新しい外部サービスを足すとき

新しいクライアント(例として Salesforce のようなもの)を追加する場面を考える。
判断は常に次の順で行う。

1. **GASらしさテスト 3 問**を満たす形に API を寄せる。
2. **プロトコル層 / ドメイン層の線**を引く。
   - 内側: 認証、リトライ、エラー正規化、文字列エスケープ、URL 構築、ページング補助
   - 外側: 個別のレコード CRUD、組織固有項目、業務特有のクエリ
3. ドメイン層は `.use()` で呼び出し側に渡す。本体はプロトコル層のみ。
4. 同種の `.use()` ブロックが 3 プロジェクト目で現れたら、任意 import のプラグインとして昇格する。
5. 日常の呼び出しが 1〜2 行で書ける肌触りを維持する。
   初回セットアップが重いものは、別動線のヘルパに隔離する。

この 5 ステップは、Salesforce に限らず、将来追加されるどんな外部サービスにも適用される。
**方針はサービスごとに変えない。線の引き方だけを明文化する。**

---

## 8. このファイルの使い方

- 新しい API / プラグイン / ヘルパを追加しようとするとき、この文書の基準(特に §1.2 と §4.3 と §5.3)に照らす。
- レビューで「これは GASらしいか?」が論点になったとき、このファイルを引用して判定を共有する。
- 将来このライブラリが "差し込みメール向けの小道具" から "何か別のもの" に変貌しそうになったとき、その変貌を意識的に選択するか意識的に拒むかの判断をこの文書に追記する。

**設計思想は書かないと忘れる。** 忘れた設計思想は再発見されるまで毎回痛みを伴う。
このファイルは、その痛みを次の読み手(将来の自分を含む)に払わせないためのもの。
