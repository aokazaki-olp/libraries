# Claudeと一緒に開発する方法 — 非エンジニア向けガイド

このリポジトリのコミット履歴・PRログ・Markdownドキュメントを分析して導き出した、
**エンジニアでなくても Claude を使って本格的なライブラリ開発を進めるための手順書**です。

---

## このリポジトリで何が行われたか（全体像）

| 期間 | やったこと |
|---|---|
| 2026-01 | コードファイルをアップロードして Claudeにレビューを依頼 |
| 2026-02 | Claudeがレビュー→修正→再レビューを9サイクル繰り返して品質をA評価に引き上げ |
| 2026-02 | Range対応の新機能追加もClaudeに依頼してPRとしてマージ |
| 2026-03 | LazyTemplateの細かい改善・利用者向けドキュメント生成 |

PRは15本。コミットは60本以上。**コードを書いたのはほぼすべてClaude**。

---

## ステップ1 — 「ルール集」を先に作る（CODING_RULES.md）

### なぜ必要か

Claudeに「このプロジェクトらしいコード」を書かせ続けるには、**一貫したルールを文書化**しておく必要があります。
毎回「こういうスタイルで」と指示する手間が省け、複数回のやりとりでもブレません。

### 何を書くか

このリポジトリの `CODING_RULES.md` を参考にすると、以下の項目が重要です。

```
1. 基本思想（どんな価値観でコードを書くか）
2. ファイル構成のパターン（ライブラリはこう作る、という型）
3. 命名規則（変数・定数の名前の付け方）
4. 禁止事項（forEach禁止、varは使わない、など）
5. エラーの出し方の統一ルール
6. JSDocの書き方
```

### Claude への依頼例

> 「このプロジェクトのコーディング規約を CODING_RULES.md としてまとめてください。
> JavaScript (GAS V8) で書くライブラリです。
> 堅牢性と可読性を重視し、ES6以降の書き方を使いたいです。」

---

## ステップ2 — コードをアップロードしてレビューを依頼する

### 初回アップロード

既存コードがあれば、ファイルをそのままリポジトリにアップロードします（`Add files via upload`）。
このリポジトリでも最初の3コミットはそのままのアップロードでした。

### Claudeへのレビュー依頼

コードが揃ったら Claude に頼みます。

> 「CODING_RULES.md のルールに従ってソースコード全体をレビューしてください。
> 問題を重要度 H（バグ）/ M（堅牢性）/ L（改善）に分類して
> REVIEW.md にまとめ、修正はPRとして出してください。」

### REVIEW.md の役割

Claudeが作る `REVIEW.md` が「作業台帳」になります。

```
- 第N版のレビューで何件の問題を見つけたか
- それぞれ修正済みか未対応か
- 設計上あえて直さないと判断したものはどれか
```

**これを見れば今の品質状態が一目でわかります。**

---

## ステップ3 — 「指摘0件」になるまでレビュー→修正を繰り返す

このリポジトリでは **9回** レビューを重ねています。

```
第1版レビュー → H3件・M9件・L6件 → 修正PR
第2版レビュー → 修正確認・追加指摘 → 修正PR
   …（繰り返し）…
第9版レビュー → 要対応0件
```

### 一回のサイクルの流れ

```
1. Claude が新しいブランチを作る（例: claude/review-xxx）
2. 問題を修正してコミット
3. PRを作成
4. あなたがPRの内容を確認してマージ
5. 次のレビューをClaudeに依頼
```

### あなたがやること

- PRの内容を読んで「このコミットは何をやったか」を確認する
- 問題なければマージボタンを押す
- 「この修正は意図と違う」と思ったら Claude に伝えて差し戻す

**コードの中身を理解する必要はありません。** REVIEW.md の「修正内容の概要」を読んで判断できます。

---

## ステップ4 — 新機能を追加する

基盤の品質が上がったら、機能追加も同じ手順です。

### 依頼例（このリポジトリのPR #7に対応）

> 「`loadFromSheetAsObjects` 関数に、シート全体だけでなく
> セル範囲（Range）を指定して読み込める機能を追加してください。
> CODING_RULES.md に従い、既存のAPIと一貫した設計にしてください。
> 追加後はレビューもお願いします。」

### Claude が自動でやること

- 新しいブランチ `claude/add-range-support-xxx` を作成
- 機能を実装してコミット
- テストコードも作成
- REVIEW.md を更新して設計・コードレビューを記録
- PRを作成

---

## ステップ5 — 利用者向けドキュメントを作る

コードが完成したら、使う人向けの説明書を Claude に作らせます。

### 依頼例

> 「`loadAsObjects.gs` の使い方ドキュメントを
> 非エンジニアでも読める `loadAsObjects.md` として作成してください。
> 具体例のコードを豊富に入れてください。」

このリポジトリでは `loadAsObjects.md` がそれに当たります。
スプレッドシートからデータを読む方法を、コード例付きで丁寧に説明しています。

---

## ブランチ・PRの命名パターン（Claude が自動で使っている）

| 名前のパターン | 意味 |
|---|---|
| `claude/review-XXX-XXXXX` | Claudeがレビュー・修正を行うブランチ |
| `claude/add-YYY-XXXXX` | Claudeが機能追加を行うブランチ |
| `feature/ZZZ` | 人が機能追加を行うブランチ |
| PR #N の `Merge pull request` | そのPRの内容がmainに統合されたコミット |

末尾のランダム文字列（`RbFhi` など）はセッションIDで、
**同じClaudeとの会話で作られたブランチ**と分かります。

---

## 全体を通じたポイント（非エンジニア向けまとめ）

### Claudeに渡すべきもの

| 渡すもの | 効果 |
|---|---|
| `CODING_RULES.md` | 「このプロジェクトらしいコード」を書き続けてくれる |
| `REVIEW.md`（累積版） | 過去の経緯を忘れず、同じ指摘を繰り返さない |
| 具体的な要件文 | 曖昧な指示より明確な成果物が出る |

### あなたが判断するタイミング

1. **PRをマージするとき** — 内容の概要を読んで承認する
2. **「設計意図によりクローズ」の判断** — Claudeが「直すべきか否か」を提示してくるので、方針を決める
3. **新機能の要件を伝えるとき** — 「何をしたいか」を言葉で説明する

### Claude が担当すること

- コードを書く・修正する
- テストを書く
- レビューして問題を分類する
- ブランチ・PRを管理する
- ドキュメントを書く

---

## 参考：このリポジトリの品質改善軌跡

```
初回アップロード時
  → H（バグ）3件、M（堅牢性）9件、L（改善）6件

第9版レビュー後
  → 全モジュール品質A〜A評価
  → 要対応指摘 0件
```

9回のレビューサイクルで、**問題だらけのコードをA評価のライブラリに仕上げた**のがこのリポジトリの歩みです。

---

*このドキュメント自体も、Git logとMarkdownファイルをClaudeが分析して生成しました。*
